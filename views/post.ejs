<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= post.title %> - HardwareTalk</title>
  <style>
    body {
      font-family: sans-serif;
      padding: 20px;
      background: #f4f4f4;
    }
    .post {
      background: white;
      padding: 20px;
      border-radius: 8px;
      max-width: 700px;
      margin: auto;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    h2 {
      margin-top: 0;
    }
    .info {
      color: gray;
      font-size: 0.9em;
      margin-bottom: 10px;
    }
    .buttons {
      margin-top: 20px;
    }
    button {
      padding: 8px 16px;
      margin-right: 10px;
    }
  </style>
</head>
<body>
  <div class="post">
    <h2><%= post.icon || '💬' %> <%= post.title %></h2>
    <div class="info">
      <span>🕓 <%= new Date(post.timestamp).toLocaleString() %></span> |
      <span>👁 조회수: <%= post.views || 0 %></span> |
      <span>❤️ 좋아요: <%= post.likes || 0 %></span>
    </div>
    <hr>
    <p><%= post.content %></p>

    <div class="buttons">
      <form method="POST" action="/like/<%= post.id %>" style="display:inline;">
        <button type="submit">❤️ 좋아요</button>
      </form>
      <form method="POST" action="/delete/<%= post.id %>" style="display:inline;">
        <button type="submit">🗑 삭제</button>
      </form>
    </div>
  </div>
</body>
</html>
